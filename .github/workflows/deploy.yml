name: Deploy a Vercel

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Publicar en Vercel
        run: npx vercel deploy --yes --prod --token ${{ secrets.VERCEL_TOKEN }}
